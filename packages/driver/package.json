{
	"name": "pob-driver",
	"private": true,
	"version": "0.6.0",
	"type": "module",
	"files": [
		"dist"
	],
	"scripts": {
		"build": "emcmake cmake -G Ninja -B build -S . -DCMAKE_BUILD_TYPE=Release && emmake ninja -C build",
		"dev": "vite",
		"preview": "vite preview"
	},
	"dependencies": {
		"@zenfs/core": "github:atty303/zenfs-core#prepare",
		"@zenfs/zip": "github:atty303/zenfs-zip#prepare"
	}
}
